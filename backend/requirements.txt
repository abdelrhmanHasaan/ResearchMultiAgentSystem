# Core API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.7

# Config
python-dotenv>=1.0

# HTTP / scraping
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
ddgs>=9.0

# PDF generation
reportlab>=4.2

# Testing
pytest>=8.0
pytest-asyncio>=0.23

# --- Optional extras -------------------------------------------------------
# Semantic vector search (install manually if desired):
#   pip install chromadb
# Without it, an in-process cosine-similarity store is used automatically.
